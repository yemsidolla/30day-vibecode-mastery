# Day 28 – Exercise

## Steps

1. Define metrics: efficiency (e.g. tasks per day per agent), project velocity (e.g. progress over time), infra cost (stub: e.g. container count × hours × rate), workload heatmap (agent × activity count or time in state).
2. Add backend endpoints or extend report engine to expose these (use stub data if needed). STOP.
3. Add Analytics panel or extend Reports: charts for trend, velocity, cost, and heatmap (table or simple grid). STOP.
4. List files created; get approval. Ready for Day 29 (CEO Dashboard).
