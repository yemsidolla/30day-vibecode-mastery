# Backend — OpenClaw

API, WebSocket, Agent Registry, Docker monitoring, Scheduler, Reporting, Governance.

**Stack:** TBD (e.g. Node.js + Express, or similar).

**Phases:** 2.1 (skeleton), 2.2 (Agent Registry), 2.3 (WebSocket), then Docker endpoint, command endpoint, health service, alerts, middleware. Build one chunk at a time per MASTER-PROMPT.md.
