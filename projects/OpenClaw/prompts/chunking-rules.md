# Chunking rules

- One prompt = one small step. Do not ask for "backend + WebSocket + Agent CRUD" in one go.
- Follow MASTER PROMPT phases: complete one step, STOP, get approval, then GO NEXT.
- If the AI gives more than asked, say REFINE or SIMPLIFY and repeat with narrower scope.
