# Day 11 – Exercise

## Steps

1. Ensure Project (and Task if needed) are in `docs/schema.md`. Add backend API for projects: list, get one, optional create/update. Use in-memory or simple DB. Small chunk. STOP.
2. Add a Project Overview panel in the frontend: list or Kanban-style cards showing project name, progress %, active agents count, deadlines, issues (or placeholder). Add a simple "health" indicator (e.g. green/yellow/red by progress or overdue). STOP.
3. Connect frontend to backend project API. List files created; confirm no overengineering. Get approval.
